{
  "name": "wifi-csi-dashboard-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "npx http-server . -p 8080 -c-1 --cors -o",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^27.0.1",
    "vitest": "^3.1.1"
  }
}
